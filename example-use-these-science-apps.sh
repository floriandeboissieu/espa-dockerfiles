local_path=`pwd`
container_path=/espa/src

# The following are examples of replacing the default application source code
# directories with local versions which can then be compiled and used in the
# container.
#cmd=${cmd}" -v ${local_path}/espa-processing:${container_path}/espa-processing"
#cmd=${cmd}" -v ${local_path}/espa-product-formatter:${container_path}/espa-product-formatter"
